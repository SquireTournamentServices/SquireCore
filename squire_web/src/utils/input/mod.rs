use std::collections::HashMap;

use yew::prelude::*;

mod text;
pub use text::*;

//mod transformer;
//pub use transformer::*;

/*
pub enum InputType {
    Text(TextInput),
}

pub struct InputBox {
    // TODO: Avoid string-typed data and make keys types with known properties
    // This should also include making the map a constant size
    data: HashMap<String, InputType>,
}
*/
